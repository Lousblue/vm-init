# vm-init
First-boot setup for a fresh Ubuntu or Debian VM. Detects the system, asks what to do - updates, base tools, a network share, IPv6, root volume, SSH hardening - then applies it. Flags and environment variables for unattended use. Made for Proxmox guests, works on any VM.
